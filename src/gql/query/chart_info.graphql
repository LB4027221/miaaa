query GET_CHART_INFO($chartId: ID!) {
  user {
    __typename
    _id
    chart(chartId: $chartId) {
      _id
      title
      __typename
      chartType
      worksheet
      report
      conf
    }
  }
}
